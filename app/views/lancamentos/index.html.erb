<div class="header">
  <h1>Lançamentos</h1>
</div>
<table>
  <thead>
    <p id="notice"><%= notice %></p>
    <%= stylesheet_link_tag  'scaffolds', media: 'all', 'data-turbolinks-track' => true %>
    <tr>
      <th>Status</th>
      <th>Ficha</th>
      <th>Customer</th>
      <th>Dtvencimento</th>
      <th>Dtemissao</th>
      <th>Dtpagamento</th>
      <th>Historico</th>
      <th>Valor</th>
      <th>Valorpago</th>
      <th>Acrescimo</th>
      <th>Desconto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lancamentos.each do |lancamento| %>
      <tr>
        <td><%= lancamento.status %></td>
        <td><%= lancamento.Ficha %></td>
        <td><%= lancamento.Customer.nome %></td>
        <td><%= lancamento.dtvencimento %></td>
        <td><%= lancamento.dtemissao %></td>
        <td><%= lancamento.dtpagamento %></td>
        <td><%= lancamento.historico %></td>
        <td><%= lancamento.valor %></td>
        <td><%= lancamento.valorpago %></td>
        <td><%= lancamento.acrescimo %></td>
        <td><%= lancamento.desconto %></td>
        <td><%= link_to 'Show', lancamento %></td>
        <td><%= link_to 'Edit', edit_lancamento_path(lancamento) %></td>
        <td><%= link_to 'Destroy', lancamento, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lancamento', new_lancamento_path %>
