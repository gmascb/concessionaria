<%= form_for(@contum) do |f| %>
  <% if @contum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contum.errors.count, "error") %> prohibited this contum from being saved:</h2>

      <ul>
      <% @contum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :status %><br>
    <%=select("Status", "Status", ['Aberto','Baixado'], {include_blank: true})%>
  </div>
    <div class="field">
    <%= f.label :Customer_id %><br>
    <%= select("Customer", "Customer", Customer.all.collect {|p| [ p.nome, p.id] }, {include_blank: ''}) %>
  </div>
  <div class="field">
    <%= f.label :emissao %><br>
    <%= f.date_select :emissao %>
  </div>
  <div class="field">
    <%= f.label :vencimento %><br>
    <%= f.date_select :vencimento %>
  </div>
  <div class="field">
    <%= f.label :Ficha_id %><br>
    <%= f.text_field :Ficha_id %>
  </div>

  <div class="field">
    <%= f.label :historico %><br>
    <%= f.text_field :historico %>
  </div>
  <div class="field">
    <%= f.label :datapagamento %><br>
    <%= f.date_select :datapagamento %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
